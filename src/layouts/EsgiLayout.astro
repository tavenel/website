---
import Layout from '@layouts/PromoLayout.astro';

const { promo, agenda, url } = Astro.props;
const title = "Modules ESGI " + promo;
---
<Layout title={title} mail="tavenel@myges.fr" agenda={agenda} url={url}>
	<slot />
</Layout>

