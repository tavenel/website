---
import Layout from '@layouts/SlideLayout.astro';

const { title } = Astro.props;
type Props = {
		title: string,
	}

import {Content as Legal} from '@courses/cicd/legal.md';
const frontmatter = {
  license: "© 2025 Tom Avenel under 󰵫  BY-SA 4.0",
  title: `Cours CI/CD - ${title}`,
  tags: [
    "ci",
    "cd",
    "ci/cd"
  ],
}

---

<Layout frontmatter={frontmatter} >

  <slot />
  <Legal />

</Layout>
