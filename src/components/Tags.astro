---
const { tags } = Astro.props;
interface Props {
	tags?: string[];
};

---
<div class="tags">
	{ tags && <span class="tags-label">Tags: </span> }
	{ tags && tags.map( (tag) => <span class='tag'>{tag}</span> ) }
</div>


