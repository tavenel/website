---
const { tags } = Astro.props;
interface Props {
	tags?: string[];
};

---
<div class="tags">
	{ tags && <span class="tags-label">Tags: </span> }
	{ tags && tags.map( (tag) => <span class='tag'>{tag}</span> ) }
</div>

<style>

.tag {
	margin-right: 5px;
	font-style: italic;
}

</style>
